# PowerShell scripts are UTF-8 without BOM.
# BOM breaks `iwr ... | iex` (PowerShell 5.1 executes the BOM bytes as a command).
# Non-ASCII chars (em-dashes, etc.) are only in comments/log strings — safe without BOM.
*.ps1 diff=utf-8
